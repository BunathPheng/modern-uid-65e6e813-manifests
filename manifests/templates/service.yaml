---
# Source: helm for kubernetes/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: modern-uid-65e6e813          # Service name
  labels:
    app: modern-uid-65e6e813
spec:
  type: ClusterIP   # Service type (ClusterIP, NodePort, LoadBalancer)
  selector:
    app: modern-uid-65e6e813         # Matches Deployment labels
  ports:
    - protocol: TCP
      port: 5173         # Service port
      targetPort: 5173  # Target container port
